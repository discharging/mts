import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#F6F4EB",
    },
    secondary: {
      main: "#FECDA6",
    },
    another: {
      main: "#FF5B22",
      light: "#FECDA6",
    },
  },
});
export default theme;
